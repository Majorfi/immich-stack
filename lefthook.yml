pre-commit:
  commands:
    prettier:
      run: prettier . --write
      stage_fixed: true
    goTest:
      glob: "*.{go}"
      run: go test ./...

pre-push:
  commands:
    goTest:
      glob: "*.{go}"
      run: go test ./...
